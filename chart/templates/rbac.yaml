apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "eos.fullname" . }}
  labels:
    {{- include "eos.labels" . | nindent 4 }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "eos.fullname" . }}
  labels:
    {{- include "eos.labels" . | nindent 4 }}
rules:
  # Plugin + Broker CRD management
  - apiGroups: ["eos.io"]
    resources: ["plugins", "plugins/status", "brokers", "brokers/status"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  # Pod management for plugin pods
  - apiGroups: [""]
    resources: ["pods", "pods/log"]
    verbs: ["get", "list", "watch", "create", "delete"]
  # Read ConfigMaps and Secrets referenced by plugins/brokers
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["get", "list", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "eos.fullname" . }}
  labels:
    {{- include "eos.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "eos.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "eos.fullname" . }}
    namespace: {{ .Release.Namespace }}
